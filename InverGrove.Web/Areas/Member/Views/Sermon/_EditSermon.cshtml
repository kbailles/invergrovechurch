<div class="modal-header">
    <h3 class="modal-title">Edit Sermon</h3>
</div>
<div class="modal-body">
    <div class="home-wrapper">
        @using (Html.BeginForm("Add", "Sermon", new {ReturnUrl = ViewBag.ReturnUrl}, FormMethod.Post, new {role = "form", novalidate = true, name = "form"}))
        {
            @Html.AntiForgeryToken()

            <fieldset class="reply">
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <label>Title: <span class="warning">*</span></label>
                        <input class="form-control" name="Title" ng-model="modalCtrl.sermon.title" type="text" required />
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <label>Speaker: <span class="warning">*</span></label>
                        <input class="form-control" name="Speaker" ng-model="modalCtrl.sermon.speaker" type="text" required />
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <label>Sermon Date: <span class="warning">*</span></label>
                        <input class="form-control" name="SermonDate" ng-model="modalCtrl.sermon.sermonDate" type="text" required />
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <label>Tags: <span class="warning">*</span></label>
                        <input class="form-control" name="Tags" ng-model="modalCtrl.sermon.tags" type="password" required />
                    </div>
                </div>
                <div class="row">
                    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
                        <label>SoundCloud Id: <span class="warning">*</span></label>
                        <input class="form-control" name="SoundCloudId" ng-model="modalCtrl.sermon.soundCloudId" type="text" required />
                    </div>
                </div>
            </fieldset>
        }
    </div>
</div>
<div class="modal-footer">
    <button class="btn btn-color btn-small" ng-click="vm.tryThis()">Save</button>
    <button class="btn btn-small btn-warning" ng-click="modalCtrl.dismissModal()">Cancel</button>
</div>